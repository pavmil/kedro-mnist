# Параметры пайплайна сбора данных.

split:
  n_test: 10000            # каноничный тест MNIST — последние 10000 строк

preprocess:
  label_col: label
  scale: 255.0             # нормализация пикселей в диапазон [0, 1]
